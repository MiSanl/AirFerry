## v1.1.4

> 本版本包含浏览器/网页发送端，以及 Android、Windows 接收端。发送端包名统一加入 `sender`，接收端包名统一加入 `receiver`，并在原生客户端包名中标明架构，避免用途和平台兼容性混淆。

### 扫码修订（重新发布）

- **多码状态修复**：Android 按 CameraX 0°/90°/180°/270° 正确映射可见四宫格；二维码完全消失时也由独立 UI 时钟及时转为暂停，不再卡在“活跃”。
- **扫码性能恢复**：补发现改为替代一帧 ROI，不再同一帧重复执行 ROI + 1080p 全帧扫描；长期缺失码位会退出高频 ROI，重新出现后自动发现。
- **共享热路径优化**：Android/Windows 正常极性 ROI 不再为缺失码逐帧做反色重试；全帧重锁仍保留 TryInvert。

### 体验与性能

- **网页全页拖放**：文件或文件夹可拖到页面任意位置；目录分批递归读取，支持并发拖放、卸载作废和读取中状态。
- **Windows 对齐 Android 扫码链路**：共享 ZXing-C++ 解码核心，多 worker 解码 + 串行 Rust 摄入，支持多码 ROI 跟踪、15 FPS 预览与 3 秒实时吞吐窗口。
- **Windows 完整接收流程**：统一 ContentStore 历史、文本/文件/bundle 结果页、原名导出和摄像头原地停止/继续。

### 安全与可靠性

- **WASM session 生命周期**：消除 epoch 失效、替换、卸载和 React StrictMode 下的双重 `free()` 风险。
- **协议输入加固**：补齐 OTI/SBN/ESI/descriptor/Frame 长度校验；C ABI raw pointer API 明确为 unsafe，并修复 assemble 输出参数泄漏边界。
- **资源预算**：对象上限统一为 32 MiB；断点 JSON 输入/输出、符号数量和字节数均有独立预算，降低合法大对象触发 OOM 的风险。
- **ContentStore 故障保护**：损坏索引先备份再停止写入，不再静默清空；Android/Windows 分享保留逻辑文件名、扩展名和 MIME，避免落成 `.bin`。
- **不可信文件隔离**：Windows 历史文件只在应用内预览；外部导出写入 MOTW，不直接 shell 执行接收内容。
- **安全停止**：Windows producer、recovery、worker、native session/camera 严格有序释放；慢驱动超时后后台持有资源，避免并发 free/Dispose。

### 构建与测试

- WASM legacy/SIMD 在隔离 workspace 构建，并用跨进程锁保护发布和消费；web 使用独立现代 WASM 快照。
- Windows 构建仅接受真实 DLL，并由固定 SHA 的 GitHub Actions 在 Windows runner 打包。
- 新增 Android 协议/helper 单测与 Windows C ABI、CRC、预览、导出测试；同步更新协议和构建文档。

### 版本

- **1.1.4**（Android versionCode=**9**）；sender/web、Rust、Windows 和 `windows.yml` 已同步。

### 产物

| 文件 | 说明 |
|------|------|
| `airferry-receiver-android-arm64-v1.1.4.apk` | Android 接收端（arm64-v8a，Android 10+） |
| `airferry-receiver-windows-x64-v1.1.4.zip` | Windows 接收端（x64，CI 构建；需安装 .NET 8 Desktop Runtime） |
| `airferry-sender-chrome-mv3-v1.1.4.crx` / `.zip` | Chrome / Edge MV3 |
| `airferry-sender-chrome-mv2-v1.1.4.crx` / `.zip` | Chrome / Edge MV2 |
| `airferry-sender-firefox-mv3-v1.1.4.xpi` | Firefox MV3 |
| `airferry-sender-firefox-mv2-v1.1.4.xpi` | Firefox MV2 |
| `airferry-sender-web-v1.1.4.zip` | 网页发送端静态站点 |
| `airferry-sender-web-standalone-v1.1.4.html` | 网页发送端单文件版（双击即可运行） |
